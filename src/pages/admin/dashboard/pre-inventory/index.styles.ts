const monthPickerGrid = {
  mt: 2,
};

const paperContainer = {
  p: 2,
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
};

const accordionSummaryBox = {
  display: 'flex',
  alignItems: 'center',
  width: '100%',
  pr: 2,
};

const chartsGrid = {
  mt: 3,
};

const gridFull = { size: { xs: 12 } };

const gridLarge = {
  size: { xs: 12, md: 8 },
};

const gridSmall = {
  size: {
    xs: 12,
    md: 4,
  },
};

const gramsPaper = {
  p: 2,
  display: 'flex',
  flexDirection: 'column',
  flex: 1,
  height: 360,
};

const salesPaper = {
  p: 2,
  display: 'flex',
  flexDirection: 'column',
  flex: 1,
  height: 360,
};

const divider = {
  my: 2,
};

const accordionMarginTop = { mt: 2 };

export default {
  monthPickerGrid,
  paperContainer,
  accordionSummaryBox,
  chartsGrid,
  gramsPaper,
  salesPaper,
  divider,
  gridFull,
  gridLarge,
  gridSmall,
  accordionMarginTop,
};
