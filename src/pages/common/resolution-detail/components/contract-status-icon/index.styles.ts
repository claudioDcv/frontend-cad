import { SxProps, Theme } from '@mui/material';

const contractStatusIconsContainer: SxProps<Theme> = {
  display: 'flex',
  alignItems: 'center',
  gap: 1,
};

export default {
  contractStatusIconsContainer,
};
