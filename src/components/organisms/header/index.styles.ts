import { SxProps, Theme } from '@mui/material';

const headerStyles: SxProps<Theme> = {
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  my: 1.2,
};

export default {
  headerStyles,
};
